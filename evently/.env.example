NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=Evently
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Default admin account created by `npm run seed:admin`.
# Change the password after your first login.
DEFAULT_ADMIN_EMAIL=admin@evently.app
DEFAULT_ADMIN_PASSWORD=Evently-Admin-2026!

# Optional: shows a "use demo credentials" button on the login page.
# Leave blank in production. Should match the values above.
NEXT_PUBLIC_DEMO_ADMIN_EMAIL=admin@evently.app
NEXT_PUBLIC_DEMO_ADMIN_PASSWORD=Evently-Admin-2026!
